---
import { seo_places2 } from "../../data/seo_places";
import { seoKeys } from "../../data/seo/debouchage";

const url = "/";
---

<aside class="space-y-6">
  <!-- --- Category ---  -->
  <div class="sidebar__single sidebar__category">
    <h3 class="sidebar__title">Nos services</h3>
    <ul class="sidebar__category-list">
      <li>
        <a href="/plomberie-sanitaire">Plomberie </a>
      </li>
      <li class="active">
        <a href="/debouchage-canalisation">Débouchage</a>
      </li>
      <li>
        <a href="/contact">Contactez-nous</a>
      </li>
    </ul>
  </div>
  <!-- --- Category ---  -->

  <!-- --- support ----- -->

  <div class="sidebar__single sidebar__support">
    <div class="sidebar__support-bg" style={`background-image: url();`}></div>
    <h3 class="sidebar__suppot-title">Une question ? Un besoin?</h3>
    <div class="mt-10">
      <a href="tel:0474 56 56 56" class="thm-btn" data-text="0474 56 56 56">
        0474 56 56 56</a
      >
    </div>
  </div>

  <!-- --- support ----- -->

  <!-- Tags -->

  <div class="sidebar__single sidebar__tags">
    <ul class="sidebar__tags-list">
      {
        Object.entries(seoKeys).map(([slug, label]) => (
          <li>
            <a href={`/service-debouchage/${slug}`} class="hover:underline">
              {label}
            </a>
          </li>
        ))
      }
      <li>
        <a href="/liens_SL_Debouchage" class="hover:underline"
          >Les autres services Débouchage</a
        >
      </li>
    </ul>
  </div>

  <!-- Cities -->

  <div class="sidebar__single sidebar__tags">
    <ul class="sidebar__tags-list">
      {
        Object.entries(seo_places2).map(([slug, value]) => (
          <li>
            <a
              href={`${url}${slug === "bruxelles" ? "entreprise-de-debouchage/debouchage/" : "prestation-debouchage/debouchage/"}${slug}`}
              class="hover:underline"
            >
              {value}
            </a>
          </li>
        ))
      }
      <li>
        <a href={`${url}liens_debouchage_villes_bruxelles`}
          >Services Débouchage pour les autres communes Bruxelles</a
        >
      </li>
      <li>
        <a href={`${url}liens_debouchage_communes_belgique`}
          >Service Débouchages pour les autres communes Belgique</a
        >
      </li>
    </ul>
  </div>
</aside>
